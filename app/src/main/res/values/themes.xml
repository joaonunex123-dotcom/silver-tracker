<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema apenas para a splash do sistema; a UI real e definida em Compose. -->
    <style name="Theme.StackingTracker" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/fundo_escuro</item>
    </style>
</resources>
